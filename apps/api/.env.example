# ==========================================
# Server Configuration
# ==========================================
PORT=3001
NODE_ENV=development

# ==========================================
# Database
# ==========================================
DATABASE_URL="postgresql://user:password@localhost:5432/trustee_portal?schema=public"

# ==========================================
# Security - CRITICAL: Generate secure secrets!
# ==========================================
# ⚠️  SECURITY WARNING: These values MUST be changed before deployment!
# 
# To generate secure secrets, run:
#   node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
#
# Generate JWT_SECRET and COOKIE_SECRET separately - they must be DIFFERENT!
#
# MINIMUM REQUIREMENTS:
# - At least 64 characters for JWT_SECRET
# - At least 32 characters for COOKIE_SECRET  
# - Must be cryptographically secure random strings
# - JWT_SECRET and COOKIE_SECRET must NOT be the same

# ❌ DO NOT USE THESE DEFAULT VALUES IN PRODUCTION!
# The application will refuse to start in production with weak secrets.
JWT_SECRET=GENERATE_WITH: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_EXPIRES_IN=24h

# Cookie secret for signed cookies (CSRF protection)
# Generate separately from JWT_SECRET
COOKIE_SECRET=GENERATE_WITH: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"

# CSRF Protection
CSRF_COOKIE_NAME=_csrf

# CORS Settings
CORS_ORIGIN=http://localhost:3000
CORS_CREDENTIALS=true

# ==========================================
# Frontend URL
# ==========================================
FRONTEND_URL=http://localhost:3000

# ==========================================
# Email (SMTP)
# ==========================================
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password
SMTP_FROM=noreply@trusteeportal.com

# ==========================================
# Supabase (Required)
# ==========================================
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_KEY=your-service-key
SUPABASE_ANON_KEY=your-anon-key

# ==========================================
# Stripe (Optional - for billing)
# ==========================================
STRIPE_SECRET_KEY=sk_test_...
STRIPE_WEBHOOK_SECRET=whsec_...
STRIPE_PUBLISHABLE_KEY=pk_test_...
